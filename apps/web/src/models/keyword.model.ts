export type Keyword = {
  keywordId: string;
  createdAt: string;
  updatedAt: string;
  resultCount: bigint;
  linkCount: number;
  adWordsCount: number;
  rawHtmlResult: string;
  body: string;
  uploadId: string;
};
